<h1>Editing todo</h1>

<% form_for(@todo, :url => list_todo_path(@list, @todo)) do |f| %>
  <%= render :partial => 'form', :object => f %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', list_todo_path(@list, @todo) %> |
<%= link_to 'Back', list_todos_path(@list) %>
